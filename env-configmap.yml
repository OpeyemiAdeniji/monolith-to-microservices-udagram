apiVersion: v1
kind: ConfigMap
metadata:
  name: udagram-env
data:
  # property-like keys; each key maps to a simple value
  POSTGRES_HOST: udagramdb.c6hzvzyu5cuj.us-east-1.rds.amazonaws.com
  POSTGRES_DB: yemidatabase
  AWS_BUCKET: yemi-udagram-bucket
  AWS_REGION: us-east-1
  AWS_PROFILE: default
  JWT_SECRET: testing
  URL: http://localhost:8100
